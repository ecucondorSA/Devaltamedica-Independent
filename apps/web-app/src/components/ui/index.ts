// Re-export UI components that are app-local (no Button here to avoid duplication)
// export { Button } from './Button' // removed: use @altamedica/ui
export { Input } from './input'
export { Label } from './label'
export { Card, CardContent, CardDescription, CardHeader, CardTitle } from './Card'