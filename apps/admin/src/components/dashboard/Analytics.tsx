'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@altamedica/ui';

export default function Analytics() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Analytics</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Analytics component is not implemented yet.</p>
      </CardContent>
    </Card>
  );
}
